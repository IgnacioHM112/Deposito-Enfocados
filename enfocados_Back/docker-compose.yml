# NOTA: Este archivo es histórico y ya NO se usa.
# El proyecto ahora funciona con XAMPP en lugar de Docker.
# Ver SETUP-XAMPP.md para las instrucciones de configuración actual.

# Contenido anterior (solo para referencia):
# version: '3.8'
# services:
#   db:
#     image: mysql:8.0
#     ...
#   app:
#     build: .
#     ...
